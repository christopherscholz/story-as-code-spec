id: short-story-mara-pov
lens: mara-pov
format: short-story
source_version: "0.2.0"

beat_position:
  beat: truth-revealed

dependencies:
  nodes:
    - id: mara
      must_exist: true
      required_state:
        at:
          frame: main-timeline
          time: "Day 5"
        properties:
          knowledge: [lighthouse, village, finn, hidden-cave, old-journal, elara]
    - id: elara
      must_exist: true
    - id: lighthouse
      must_exist: true
    - id: hidden-cave
      must_exist: true
    - id: old-journal
      must_exist: true

  edges:
    - id: mara-at-lighthouse
      must_exist: true
    - id: mara-finds-journal
      must_exist: true
    - id: mara-learns-truth
      must_exist: true
    - id: mara-gives-journal
      must_exist: true

  reveals:
    - node: hidden-cave
      revealed_to: mara
      at: "Day 4"
    - node: elara
      revealed_to: mara
      at: "Day 5"
