{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "description": "Reusable definition for external file references. Any array item or top-level section that supports modular file splitting uses this schema.\n",
  "type": "object",
  "required": [
    "$ref"
  ],
  "properties": {
    "$ref": {
      "type": "string",
      "format": "uri-reference",
      "description": "Path to an external YAML file containing this item"
    }
  },
  "additionalProperties": false
}
