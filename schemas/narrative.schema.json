{
  "$schema": "https://story-as-code.dev/schemas/v0.4.0/narrative.schema.json",
  "type": "object",
  "description": "The narrative layer — defines how the world is told: perspective, selection, and presentation. The narrative layer does not define its own truth. All facts live in the world layer. Narrative schemas only select, filter, and arrange world content for storytelling.\n",
  "properties": {
    "lenses": {
      "type": "array",
      "items": {
        "$ref": "lens.schema.json"
      },
      "description": "Narrative perspectives — filter and interpret world content (define no own truth)"
    },
    "formats": {
      "type": "array",
      "items": {
        "$ref": "format.schema.json"
      },
      "description": "Output format definitions — purely about presentation, no content relevance"
    },
    "beats": {
      "type": "array",
      "items": {
        "$ref": "beat.schema.json"
      },
      "description": "Story beats — hierarchical narrative building blocks. Beats select world graph elements and add dramaturgical structure. Vertical chaining via parent groups beats within a storyline; horizontal chaining via threads connects beats across storylines.\n"
    },
    "devices": {
      "type": "array",
      "items": {
        "$ref": "device.schema.json"
      },
      "description": "Narrative devices — rhetorical connections between beats (foreshadowing, red herrings, etc.)"
    },
    "threads": {
      "type": "array",
      "items": {
        "$ref": "thread.schema.json"
      },
      "description": "Thematic threads and motifs — horizontal chaining of beats across the storyline hierarchy. Connect beats from different storylines through shared thematic meaning.\n"
    },
    "variants": {
      "type": "array",
      "items": {
        "$ref": "variant-meta.schema.json"
      },
      "description": "Parallel world version metadata"
    }
  }
}
