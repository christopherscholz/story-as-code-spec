{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "A type definition — defaults and required properties for a given element type.",
  "required": [
    "id",
    "name"
  ],
  "properties": {
    "id": {
      "type": "string",
      "examples": [
        "CHARACTER",
        "LOCATION",
        "SPATIAL",
        "KINSHIP"
      ]
    },
    "name": {
      "type": "string",
      "examples": [
        "Character",
        "Location",
        "Spatial Relationship"
      ]
    },
    "applies_to": {
      "type": "string",
      "description": "Element kind this type applies to (e.g. NODE, EDGE)",
      "examples": [
        "NODE",
        "EDGE",
        "DEVICE",
        "FORMAT",
        "THREAD"
      ]
    },
    "description": {
      "type": "string",
      "examples": [
        "A sentient being that can act, speak, and have relationships. Characters drive the story through their choices and conflicts.",
        "A spatial or containment relationship between two nodes (e.g., located-in, part-of, borders)."
      ]
    },
    "defaults": {
      "type": "object",
      "description": "Default property values for elements of this type"
    },
    "required_properties": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Required property keys for elements of this type"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "examples": [
    {
      "id": "CHARACTER",
      "name": "Character",
      "applies_to": "NODE",
      "description": "A sentient being that can act, speak, and have relationships.",
      "required_properties": [
        "age"
      ],
      "defaults": {
        "alive": true
      }
    },
    {
      "id": "SPATIAL",
      "name": "Spatial Relationship",
      "applies_to": "EDGE",
      "description": "A spatial or containment relationship between two nodes.",
      "defaults": {
        "directed": true
      }
    }
  ]
}
