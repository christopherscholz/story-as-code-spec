# Type Schema

A type definition for graph and narrative elements. Types are defined once in `definitions` and referenced by ID from any element's `type` field.

{%
  include-markdown "./generated/type.md"
%}
